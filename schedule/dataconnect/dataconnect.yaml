specVersion: "v1"
serviceId: "schedule"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "schedule-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
